<Window x:Class="LanDesk.UacCredentialsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="UAC Credentials" Height="180" Width="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="Enter credentials for the remote UAC prompt (host must run as Windows Service):" 
                   TextWrapping="Wrap" Margin="0,0,0,10"/>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,6">
            <TextBlock Text="Username:" Width="80" VerticalAlignment="Center"/>
            <TextBox x:Name="UsernameBox" Width="220" VerticalAlignment="Center" Padding="4,2"/>
        </StackPanel>
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,12">
            <TextBlock Text="Password:" Width="80" VerticalAlignment="Center"/>
            <PasswordBox x:Name="PasswordBox" Width="220" VerticalAlignment="Center" Padding="4,2"/>
        </StackPanel>
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Click="OkButton_Click" IsDefault="True" Padding="20,6" Margin="0,0,8,0"/>
            <Button Content="Cancel" IsCancel="True" Padding="20,6"/>
        </StackPanel>
    </Grid>
</Window>
